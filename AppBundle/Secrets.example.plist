<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<!-- Copy to `Secrets.plist` and fill in. `Secrets.plist` is gitignored;
     scripts/app.sh merges its keys into the assembled bundle's Info.plist, so
     the credential reaches Bundle.main without ever reaching git.

     These are the Google Cloud OAuth 2.0 *Desktop app* client credentials the
     mail widget signs in with — the same pair psymail-mini keeps in its own
     Config/Secrets.xcconfig. Create them at
     https://console.cloud.google.com/apis/credentials with the Gmail API
     enabled. Without this file the app builds and runs; the mail widget just
     shows its signed-out state. -->
<dict>
	<key>GoogleClientID</key>
	<string>your-client-id.apps.googleusercontent.com</string>
	<key>GoogleClientSecret</key>
	<string>your-client-secret</string>
</dict>
</plist>
