# handoff 2026-09-09

User is day-testing ccp-a77s (notes Error-then-endless-Syncing) and will come back. ccp-o3k + ccp-hw0 shipped this session; a77s stays CLAIMED for their return — do not grab it.

When they report back: the discriminator is whether a 30s-open panel lands on Synced. If it still spins, re-instrument NotesAdapter (recipe: file-scope syncTrace() appending to /tmp/ccp-sync-trace.txt plus Logger, log points at activate/deactivate/schedulePull/pullAll entry+gate+clock-fail/per-pad fetch/verified — the exact lines are in git history, they were reverted cleanly) and watch a stuck round; os_log alone never surfaced (Logger emits nothing visible from this binary — file tracing is what worked).

What tonight proved: Craft healthy (clock 1.2s, all fetches <1s, docs tiny), a full 5-pad round traced at 4s VERIFIED, so o3k is exonerated. Earlier rounds died after ~1 pad with an idle process and no connections — no code exists between pads that can stall, so those deaths were cancellation (deactivate/schedulePull/credential-observer are the only three). First-open Error is the clock read failing, retried in 30s. ccp-dz0's phantom self-dismissal is the prime suspect if rounds keep dying young without the user closing anything.

Watch items: a parallel session is filing/claiming Pomodoro beads live (ledger drifts under you — .beads/issues.jsonl left dirty deliberately, do not commit their beads); live defaults domain is com.controlcenterpro.ControlCenterPro, NOT the stale ControlCenterPro domain (remembered). Clean build running, gate 753 green, tree clean except the ledger file.
