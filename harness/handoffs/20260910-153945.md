# handoff 2026-09-10

ccp-nbg3 still broken after dc48783: second right-click after a menu dismiss still opens the panel, not the menu. The deferred-popUp theory is dead — don't re-try it.
Was mid-thought on the trace's real lesson: press 2 arrived as rightMouseUp (type 4), and NOTHING in the deferred version changed that path. Next session: re-add the stripped clickDebug to handleStatusItemClick, get a fresh trace on the current tree, and take seriously that the fix may be matching rightMouseUp as right-click (with a guard against the double-fire the mask analysis feared) or a local rightMouseDown monitor that owns detection outright. Finding 1's warning in the last correctness review called this exact outcome.
