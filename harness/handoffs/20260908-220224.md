# handoff 2026-09-08

Closed ccp-2zi.4 (`82929d1`, gate 737 green) — Craft sync state sits behind
`CraftSyncStore` and the adapter's handle is private. Next: reassess plan §4.1
against the working seam, or take ccp-hw0 (small, scoped); ccp-3td is unblocked
now that the extraction is over.

Details for whoever picks this up:

- The deliverable: `Sources/CCPKit/Craft/CraftNoteDestination.swift` owns the 7
  maps + space id (keys/format unchanged, no migration). `NotesAdapter` holds
  `private let craftDestination: any CraftSyncStore`. Public Craft-noun API is
  gone except UI-needed `conflicts(for:)`/`dismissConflict`; `conflictsVersion`
  stays on the adapter with delegate-and-bump wrappers. `FakeCraftSyncStore`
  proves the seam across a full push and pull
  (`Tests/CCPKitTests/NoteDestinationTests.swift`). Observation test added for
  the `_ = conflictsVersion` read (memory: `observation-dependency-test-pattern`).
- The shape changed mid-work under taste review, and the reasoning matters:
  the first cut had a generic `NoteDestination` protocol, which the reviewer
  rightly called false generality — no vault backend (paths, no ids) can
  conform to `sidecar(for:)`/`stashIDs(for:)`. It became `CraftSyncStore`
  (Craft-scoped, Store suffix). If a future session is tempted to re-generalize
  it for Obsidian, don't: the §4.1 operation protocol is the place, and the
  store's doc comment says so. Same review made the handle private with tests
  owning + injecting their destinations — no production code reaches through.
- Stated residual (also in the property comment): the engine's own code still
  speaks Craft nouns *through* the seam (`CraftClient`, `CraftPull`,
  `BlockSidecar` in push/pull). Hiding those is the deferred §4.1 work, not a
  regression. Correctness review shipped clean; the three taste items were
  fixed in-commit.
- Watch item: a live parallel session was editing `../psymail-mini` tonight
  (uncommitted `CalendarService.swift` broke our build for ~10 min, then
  settled). If `scripts/verify.sh` ever fails inside PsymailKit files, check
  the sibling's `git status` before touching anything — that tree is not ours.
- I'd pick up next: ccp-hw0 (to-do round-trip test on the splitter path the
  seam just sealed — small and now well-placed), then the §4.1 reassessment
  before ccp-2zi.7/ccp-occ, which both consume the reshaped surface. ccp-o3k
  (pad history) overlaps the preserve-then-adopt path, so sequence it after
  the reassessment, not alongside.
