# handoff 2026-09-08

Planned the notes storage/sync arc, wrote none of it: docs/notes-storage-plan.md
(43e7471) is the build order. ccp-2zi.4 returned to open — its description still
says "no local truth" and contradicts the plan; rewriting it is step 4 and
should honestly happen first.

What I'd pick up: ccp-3me4 (P1). The rescue path consumes the backup without
persisting it AND never sets isStoredDocumentUnreadable, so a later edit
destroys both copies. Then ccp-mz2x (P1), a two-line guard copied from
StickyStore:61.

Mid-thought on the thing the plan can't settle: the user is considering
Obsidian, and a vault IS a folder of markdown. If local truth becomes
one-file-per-note, the folder is both the store and the vault and most of the
"integration" evaporates. An earlier review said one notes.json and called
per-note files over-engineering — decided before Obsidian was on the table, so
I don't trust it any more. Didn't chase it because §1.1 (one destination or
several?) has to land first, and that's the user's call. Resist picking a
storage format before they answer; getting it wrong is a second migration on
their real notes.

The bit that still nags: I deferred the protocol twice on "no second backend
exists," and the user named one in the same breath as approving the deferral.
The invariant in step 7 (no Craft noun escapes CraftNoteDestination) plus a
FakeNoteDestination is my answer to that, and it's untested — if the fake turns
out awkward to write, the deferral was wrong and the seam should be built.

Also: four review rounds, four different confident verdicts, and the variable
was my prompt rather than the code. bd recall
a-reviewers-verdict-follows-its-prompts-emphasis before invoking a reviewer.
