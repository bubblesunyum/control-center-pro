# handoff 2026-09-11

Shipped ccp-rjpy (shared OwnerOnlyFileWriter) and ccp-bfq8 (usage survives a limited setup-token via OAuth fallback + verdict-only backoff). User confirms Claude usage is back.

Was mid-thought on nothing — tree clean, ledger settled. If usage breaks again, `bd recall claude-setup-token-rate-limits-usage-endpoint` before anything else.
Next I'd pick up: ccp-d9n2 (sticky sync follow-up, now sits on the shared writer) or ccp-2zi.7 (sync status in header, epic to 18/19).
