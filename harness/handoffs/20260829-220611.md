# handoff 2026-08-29

Closed ccp-veh (d5a8092) — the split landed, 4148 → 3798 tokens a session.
A local agent had done the AGENTS.md half and stopped; CLAUDE.md was untouched,
so both files carried the same four sections and the cost went *up* until I
finished it. Worth checking that number before believing a split is done.

## What was actually wrong

The agent's AGENTS.md was fine — a faithful install of `template/AGENTS.md`,
placeholders substituted. The gap was everything downstream of it: CLAUDE.md
still held Review / Commits / Work tracking / Build & Test verbatim, there was
no `@AGENTS.md` import, and no `opencode.json` — so AGENTS.md's line about
opencode loading it through that file was describing a file nobody installed.
Fixed by installing `template/opencode.json` rather than editing the sentence.

`scripts/context.py`'s budget comment claims "CLAUDE.md imports AGENTS.md so
Claude Code pays for both" — that was *false* until this commit and nothing
caught it. It's true now.

## Mid-thought

CLAUDE.md's new `tracks: Package.swift` is the weakest part of this. The
Architecture section's real claim — the 38 excluded files under Core/, Services/,
Support/ — does live in Package.swift, so a change there should make the doc
stale. But most of CLAUDE.md (philosophy, naming, animation) tracks nothing and
can't; it drifts against taste, not against a file. I don't have a better answer
and left it. If it turns out to fire noisily on unrelated Package.swift edits,
dropping the tracks comment is fine — see ccp-ojz, which is already about
context.py's tracks handling being wrong in a different way.

Did not run the reviewers — the user said not to this session.

## Next

**ccp-g7l** (filed today, P2) is the honest sequel: ccp's harness scripts are all
behind harness-starter, and there's no `scripts/opencode-agents.py` or
`.opencode/agent/`. So an opencode session can now read the contract but cannot
run reviewer-taste / correctness / design. "opencode-ready" is half-true until
that lands. The starter's verify.sh gates on it; ours doesn't.

Otherwise the app itself still hasn't moved — ccp-lr7.1 (the CCPWidget protocol)
is the real work sitting in ready, and every harness session defers it again.

`.vscode/launch.json` is untracked and deliberately not committed.
