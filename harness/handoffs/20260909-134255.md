# handoff 2026-09-09

Shipped ccp-mp11 (Focus pomodoro, `c24d06a`, gate 777 green) but never saw it render — screen locked all session, captures were lock wallpaper. ccp-aelh is the screenshot design review for whoever is here with an unlocked screen.

Two things I'd want that session to know: the menu-bar countdown drives `store.tick()` itself (by design — the store ticker stops with the panel), and I briefly overwrote the user's layout.json for a capture then restored it byte-identical (verified) — don't repeat that, `never-edit-layout-file` means ask first.
