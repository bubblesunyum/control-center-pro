# handoff 2026-09-05

Shipped rocket-hide (ccp-y60f) + auto-relaunch plugin (ccp-4bgz, commit 2c0479d) — plugin loads on next opencode start.
Open next: ccp-fyg7 (plugin has no session attribution; a subagent idle can relaunch a mid-refactor tree).
Tree holds another session's Craft work in flight (ccp-2zi.5 files dirty) — stage explicitly, never sweep.

Mid-thought on nothing; both threads closed clean. If fyg7 gets picked up, the
cheapest scoping signal is probably "tree clean" (subagents never commit) rather
than anything in the session.idle payload, which is just a sessionID — but that
delays relaunch to post-commit idles, so it changes the trigger contract.
