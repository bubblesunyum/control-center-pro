# handoff 2026-09-02

Closed ccp-y7z (74e673a) — Files widget drag-out. WidgetFileRow now vends ShelfStore.fileURLs + string fallbacks via NSItemProvider, gated by isPanelEditing bool. Panel's reorder gesture only installed when editing — fixes swallow. Ghost files no longer promise dead URLs. Selection via pinnedFill/pinnedStroke + RowValue a11y. Verified 168 tests.

Reviewers on HEAD~1: taste flagged conditional-gesture duplication and design-system literals (now fixed to isPanelEditing bool, Radius.sparkline, pinned tokens); correctness flagged ghost-link path "/etc/hosts" false positive, main-thread temp writes, tap/drag exclusivity, private IsPanelEditingKey propagation, non-isolated store access; design flagged missing loud-state captures (none at HEAD~1 — rest state only) and trailing glyph size mismatch.

Was mid-thought on reviewers' deferred items: move drag provider to ShelfStore.dragProvider(for:) to collapse ViewModifier nesting, extract shared FileThumbnailView, add PanelEnvironment.swift. Next: ccp-caq (psymail glass), ccp-rh9 (review.sh stale captures), or ccp-lst (Psymail URL) — all ready. No claimed beads.

