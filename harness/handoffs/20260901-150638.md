# handoff 2026-09-01

Shipped e2af448 — 300pt lanes, one WidgetHeader for every card, shelf items pin.
Mid-thought on the shelf card's chip row: it is a horizontal scroll capped at 6
chips with a "+N", and now that the well is gone the cap is the only thing
saying there is more. A second row would read better than the "+4" does.

Two things I decided against and still half-want:

- The floating shelf's own header still hand-rolls its count badge (a plain
  Capsule at 0.18) rather than using CountBadge, because it is a window and not
  a widget. It sits right next to the card's version in a screenshot and they
  are visibly different. Either it is the same badge or the difference should be
  deliberate; right now it is neither.
- clear() spares pins, so the Clear button disables when only pins remain. That
  is correct but silent — nothing tells you why the button went dead. An empty
  "only pinned items left" affordance, or a Clear All in a menu, would say it.

The correctness reviewer re-found ccp-cvh from a new angle: the review packet
excludes Tests/*, so no reviewer ever sees a weakened assertion. This diff had
two real test changes, neither visible in the packet. Worth raising the priority.

Uncommitted at close and not mine: .claude/HARNESS.md, .claude/skills/graphify,
.claude/skills/workflow, .graphifyignore, AGENTS.md, dashboard/state.json.
They changed under me mid-session — check before folding them into anything.

Next: ccp-8ld.3 (Quick Toggles beyond hidden files) is the biggest visible gap;
the toggles card is one lonely button in a 300pt lane now.
