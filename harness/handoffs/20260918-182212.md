# handoff 2026-09-18

Paused the Passwords widget (ccp-hhzm, now backlog): user likes the card, but local-only saves aren't useful without iCloud sync. Unblocker is ccp-5hxj — needs the real Team ID + `--entitlements` in app.sh; dev builds can never sync by construction.

Closed ccp-16qo (the -34018 fallback). 765 green, everything of mine committed (e9284d8, 43f221e). The other uncommitted files in the tree are not mine — left alone.

If sync ever gets built: the open question from this session is whether file-keychain (non-sync) items surface in Passwords.app — the user never confirmed before pausing.
