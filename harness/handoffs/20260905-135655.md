# handoff 2026-09-05

Closed ccp-b64z (Tools Rocket button) and ccp-benx (nil-AXDescription matcher fix) — user tap-tested, menu opens, happy.
Mid-thought on nothing; the Rocket thread is done except ccp-vjwy (AX timeout), ccp-qpyl (install staleness), ccp-otea (design capture).

Two things I'd warn the next session about: (1) osascript AX properties lie — System Events synthesizes `description` where raw AX returns nil; always verify against the raw tree (/tmp/rocketax.swift pattern). (2) The user's running app is `build/Control Center Pro.app` via app.sh — never debug against the raw .build binary, it's a different TCC identity.
