# handoff 2026-09-18

Paused ccp-3ohm mid-diagnosis: browser approve works, token exchange gets `invalid_grant: "Invalid 'code'"`. Shape exonerated (scopes/encoding/state/URLs all CLI-identical now); prime suspect is tab confusion (several authorize tabs + rebuilds mixing verifiers) — needs one sterile cycle with logs watched.
Was mid-thought on whether the 92-char logged code's `#` means the copy button emits `code#junk` — split-replays without `state` got "Invalid request format", so that probe was inconclusive, not negative.
Do not commit with the TEMPORARY exchange logging in ClaudeTokenClient.swift still in. ccp-5tuh (clipboard image rows) rides uncommitted in the same tree — separate commit.
