# handoff 20260905-181739

Next: ccp-qzzt — return must read as a new block on a SINGLE newline; the \n\n era is over by user verdict. Lead is hard-break syntax ("  \n" + splitter learns it as the boundary); trailing-space fragility is the thing to spike first.
Was mid-thought on nothing code-side — tree clean at 87df60e, gate 412 green, app relaunched.
If the next session touches the pad and sync is parked, check for the Keychain Allow prompt first — a fresh build re-prompts and it blocks launch (remembered as rebuilt-app-reprompts-keychain-access).

Commits: 63fed69 (post-then-move head inserts, ccp-gfe5), cc15670 (bare return = paragraph break, ccp-inoq), 87df60e (Craft list returns, ccp-1amf). All reviewed, all green.
Ledger: ccp-gfe5/ccp-inoq/ccp-1amf closed; ccp-qzzt filed with the design; ccp-2zi.6 carries the pull-join rules (blank lines, single for list runs — revisit under qzzt).
Live-proven: head-insert order, return/list behavior in-app by the user. Still owed: block-gap/line-height visuals (folded into qzzt now, not a separate tune).
Suggested skills: none beyond workflow.
