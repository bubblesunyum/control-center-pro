# handoff 20260905-153200

Push engine (ccp-2zi.5) is built, triple-reviewed, committed on main — but NOT closed: it needs the vendor POST-position excerpt + a live throwaway-doc A/B before the wire shapes are confirmed. ccp-2zi.5's notes hold the full pending list; the user owes the excerpt (URL redacted) and an empty doc id.
Was mid-thought on nothing structural — the open questions are all live-confirmation, not design. The two reasoned non-fixes (within-group split attribution, first-sync-to-nonempty guard) are documented on the bead; don't let a future review re-litigate them without new evidence.
Next: close .5 with the excerpt + A/B feel, then ccp-2zi.6 (pull) — the sidecar contract it builds on is frozen in BlockSidecar.applyingPush.
