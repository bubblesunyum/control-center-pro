# handoff 2026-09-08

Next is ccp-2zi.4.2 (files+index notes truth) — OPEN, unclaimed, design locked with the user, ready to build. Everything below is the pickup.
Closed tonight: ccp-56b7 (`aefd599`), ccp-2zi.4.1 (`f2e3032`); gate 702 green; tree committed.

## Pickup: ccp-2zi.4.2 (P0)

`bd show ccp-2zi.4.2` holds the design; user locked the open points 2026-09-08:

- **Index stays in UserDefaults** (new key, Codable struct: version, selectedID,
  ordered pads `[{id, filename, name, modifiedAt, closed}]`, durable `dirtyPadIDs`
  which closes ccp-q3nd on landing). Keeps the steps 1–4 rescue semantics for free.
- **Folder holds only `.md`**: default `Application Support/Notes/`, Settings row
  repoints at a vault subfolder (switch moves files). `<title>.md`, raw pad text,
  no frontmatter, dup titles get `-2`, renames rename the file via the index.
- **Edges**: trash deletes the file, close keeps it, on-disk files never
  auto-adopt (later work), missing file → pad survives with empty text.
- **Migration**: UserDefaults blob → files+index once, verify, clear old keys;
  carry the `pads`/`notes` LegacyCodingKeys. Craft bookkeeping keys untouched.
- Stale line to fix when there: ccp-2zi.4's description still says
  "Index design still open" — it isn't anymore.

Start in `Sources/CCPKit/Adapters/NotesAdapter.swift` (`persist`/`flushSave`/
`scheduleSave`, `closedNoteIDs`, `dirtyPadIDs`, `documentKey`/`rescueKey` —
re-grep, tonight's deletions shifted lines). Per-file atomic writes on flush;
load stays synchronous (100ms panel budget, a handful of small files is fine).
Claim with `bd update ccp-2zi.4.2 --claim`; one commit; gate; standing review
pass (`scripts/review.sh` + taste + correctness); prove new tests fail without
the fix (stash-Sources trick). Never touch `Sources/Vorssaint/`.
