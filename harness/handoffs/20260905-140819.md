# handoff 2026-09-05

Closed ccp-qahu (app.sh -600 flake) in 5076d95 — bare `--launch` works again.
Left uncommitted widget edits + .beads churn that aren't mine (another session closed ccp-klbk / filed ccp-gowe mid-session) — don't touch.

Old memory `bare-scripts-app-sh-launch-passes-empty-args` (--show-panel workaround) is obsolete; new key `app-sh-launch-minus-600-fixed` has the fix. Librarian pass should retire the old key (needs user sign-off).
Next: ready list top is the Notes epic (ccp-ydo).
