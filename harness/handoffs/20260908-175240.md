# handoff 2026-09-08

Steps 1–4 of docs/notes-storage-plan.md are shipped (7 commits, gate 697 green).
Next is step 5 (`JSONFileStore` rescue + files+index notes format) — needs a bead
filed first and the index design; do not start it without both.

## Completed (all closed with reasons; commits name beads)

- Step 1, ccp-3me4 (`e8671fc`): rescue branch re-commits + sets unreadable flag.
- Step 2, ccp-mz2x (`a4c2a59`): shelf emptiness guard copied from StickyStore.
- Step 3, four commits: ccp-o2qs (`d864029`, cleared pad skips so push owns delete),
  ccp-i0wm (`8a6dbfd`, conflict sidecar advances before post-POST re-read bail),
  ccp-5ex4 (`a0f0d40`, drop falls back to selected note),
  ccp-bma3 (`dce59ff`, legacy title overwrite records a conflict entry).
- Step 4, ccp-r3el (`381531f`): one sync round at a time — `isPullInFlight` /
  `needsPullAfterFlight`, shared `drainAfterFlight()`, extracted `schedulePull()`.
  Review caught pull-vs-pull double-stash mid-pass; fixed in the same commit with
  a third latch test. New tests proven to fail without the gate (7 failures).
- Step 4b: ccp-2zi.4 rewritten (title + description) — keep local truth,
  folder-of-markdown + index, several-destinations. Rides this commit's export.

## User decisions now on record (in ccp-2zi.4's description)

§1.1 several destinations at once (§4.2 settled: never collapse the seven keys);
§1.2 folder of markdown + small index (skip `notes.json`, no second migration).

## For the next agent

- Step 5 needs a bead (hook enforces one per commit) and breaks
  `JSONFileStoreTests:46`, `StickyStoreTests:196` by design. The durable-dirty
  corner ccp-q3nd (P3, filed from r3el review) belongs with the index design.
- Step 6: claim ccp-egd, narrow cuts only (document types + statics file).
  Step 7 needs its own bead. ccp-jvu3 resolves with §1.1 (backend+space keys).
- Do not work §6-listed beads in parallel with the sequence (plan §6).
- Test-author gotcha: pull-suite `steadyPad` stores the title baseline, which is
  what makes its flush a true no-op — a probe without it spends scripts and
  misleads counts. Latch pattern for serialization tests lives in
  CraftPullSyncTests (`Latch`, three `ccp.sync.*` tests).
- Suggested skills: workflow, beads, agentic-review, graphify.
