# handoff 2026-09-01

Closed ccp-78w and ccp-mzs — global shortcut + Settings window, verified by
bubbles against the real app (Cmd-Backtick records). The bug worth remembering
is in bd: system-claimed-combos-need-the-recording-tap.

Was mid-thought on whether GlobalHotkey should live in CCPKit at all. It is
Carbon, not AppKit, so it clears ccp-ilk's bar — but SettingsStore now owns
config *and* the thing that registers it is wired by hand in AppDelegate's
withObservationTracking. That re-arm is the third copy of the pattern
(ControlPanelController has two). If a fourth shows up, it wants to be a small
primitive rather than another hand-rolled loop.

Rejected, and it still tempts: reusing upstream's HotkeyManager instead of
writing GlobalHotkey. It is a singleton hardwired to DefaultsKey.keepAwakeShortcut
and hotkey id 1, so using it would have meant either editing upstream or letting
CCP's shortcut masquerade as Keep Awake's. Ours registers under signature 'CCPP'
and checks it in the handler, which is what keeps it from swallowing the shelf's
key.

Left undone: the design reviewer wants two captures that do not exist —
ccp-28d. The status item's right-click menu has never been screenshotted (a
menu the agent cannot open, so capture.sh would need a synthetic click), and
neither has the recorder's recording state or the "another app has that
shortcut" footer. scripts/capture.sh --settings now shoots the whole display
scaled down rather than a corner crop, because the window centres and guessing
where it landed produced two screenshots of wallpaper.

Next: ccp-28d for the missing captures, or ccp-c2m (launch at login) which is
the other half of what a Settings window is for and now has a place to live.
