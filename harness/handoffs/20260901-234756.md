# handoff 2026-09-01

Closed ccp-cne — Shelf→Files: folder.fill, vertical ShelfList/ShelfRow + WidgetFileRow with 42×56 portrait QL thumbs, 2-line titles, no file-type subtitle, no card bg (pinned same as regular). Both dashboard widget and floating panel now list, clear button header-plain left of open. Build+tests pass, app relaunched (53134) after thumbnail tweaks.

Was mid-review on taste/correctness packet /tmp/ccp-review-packet.md — duplication of FileThumbnailView + ShelfItem+Display helpers, dead subtitle vars, double QL fire (onAppear+task), wrong NSScreen.main scale, singleton shared vs @Environment, main-thread NSImage decode, a11y missing on WidgetFileRow. Filed as follow-up, low-priority P2 — not blocking ship. If picking up, extract FileThumbnailView + Layout constants + async task(id:) per row.

Reviewers flagged AppKit in CCPKit (ccp-ilk) and design tokens (ccp-uet) still open — unrelated to this change.

Next: optional follow-up bead for reviewer fixes or new widget work.
