# handoff 2026-09-07

User stopped mid sticky-follow-up (ccp-rlql, still claimed, note holds the state): drag still jitters and the hover grip never appears since the edge-ring rewrite (2a97953). Gate green, tree committed, machine left clean.
Was mid-thought on whether onContinuousHover's location space is what the corner-rect math assumes — check that first; the old 1s-hover zone in f1ac31a demonstrably showed the grip, so the reveal mechanism (not the grip view) is what's broken.
If picking up: validate the isDragging mouse-through theory with one synthetic CGEvent drag while unlocked before touching the gesture again — second blind iteration is what burned this session. The Always-scrollbars global and the probe sticky are reverted; per-app WhenScrolling is still unproven and unencoded.
