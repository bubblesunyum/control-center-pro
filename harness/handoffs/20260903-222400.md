# handoff 2026-09-03

Closed ccp-35c — clipboard hide+paste via panel env + CGEvent Cmd+V, count+Clear now in header accessory.

Was mid-thought on paste reliability: initial implementation hid but didn't paste because click coordinates used bottom-left vs CG top-left (row is 13pt tall at panel 656,295 → CG 1186,342 works, 1186,487 misses). Fixed by verifying with CGWindowList bounds (panel 400,47,1392,936) and file-trigger debug then sweeping clicks. Also removed footer, kept pasteTarget remembered at present() and posted after 0.12s.

If paste fails in the wild it's likely pasteTarget nil (frontmost was accessory/not regular) or CGEvent not trusted — check AXIsProcessTrusted and rememberPasteTarget logs.

Next: ccp-518 / ccp-2yy / ccp-skd are ready P2s, or pick up gallery maxHeight extraction (ccp-urk).
