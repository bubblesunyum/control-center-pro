# handoff 2026-09-09

ccp-th8k still open and still reproducing per user: outside clicks don't exit edit mode. Three traces, zero failing evidence — every logged outside click arrived after edit had exited and hid correctly.
Was mid-thought on whether the global monitor is somehow blind only while editing (it delivers fine out-of-edit, same process) vs the clicks never leaving our window (mouse-through transitions logged alive, lanes frame sane). The discriminator is one trace of an outside click while visibly wiggling — asked, not yet received.
TEMP-DEBUG ccp-th8k logging is still in EditPill.swift, StatusItemController.swift, ControlPanelController.swift: pull it before committing, then re-run the review pass (taste+correctness were clean on this shape). Tree uncommitted, gate green at 786. Don't relaunch/capture while the user is active — hand commands.
