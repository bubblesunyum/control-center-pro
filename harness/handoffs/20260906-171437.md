# handoff 2026-09-06

Clipboard-to-notes arc done and user-verified live twice: rich capture (ccp-a5ss), drag into notes (ccp-p0tn, absorbed ccp-xc8), markdown-on-drop (ccp-k9yi). Tree green, all committed, nothing in progress.

Was mid-thought on nothing — user asked for the handoff. If they come back: the honest residual is the RTF-only-garbage drop (accepts, lands nothing; documented in canResolveDrop) and multi-select rich paste staying plain (deferred per user call). Both are filed nowhere — `bd q` them if either gets real.
