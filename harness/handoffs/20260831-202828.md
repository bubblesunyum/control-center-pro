# handoff 2026-08-31

Closed ccp-ul1 e929e9b — drag hang/jitter fixed with frozen snapshot + panel-level gesture + deferred single commit + window gate. Much more stable now.

Was mid-thought on window resize pinning: lift still triggers one place() for new-lane offer (trailing-aligned HStack keeps screen pos stable), layout changes gated while isDragging. Gap placeholder may clip if moving compact into tallest lane makes it taller than pre-drag max, but rare and less bad than thrash. Worth validating with 3 lanes of mixed heights.

Next: ccp-m53 P2 to migrate to reorderContainer when macOS 26 GA, or ccp-8ld widget set. Ledger has 4 harness P1s in_progress (opencode) unrelated to drag.

