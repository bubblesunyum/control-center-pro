# handoff 2026-09-05

Messages epic parked at P3 (ccp-9zvd); spike verdicts live in ccp-a7mo notes + ccp-41t2 tapback rationale.
Next: P1 Notes/Craft rows top `bd ready`; Messages resumes at ccp-fidt when unparked.

Tree was already dirty when I arrived (Package.swift, NotesAdapter, BlockSidecar) — none of it mine, left untouched; my only repo writes are the bead ledger itself. No gate run, nothing to commit.
The display-only ceiling (no tapback send, no inline replies) is also in `bd remember` as messages-tapback-has-no-public-surface.
