# handoff 2026-09-10

Shipped ccp-i13i + ccp-ua73 (sync-status popover owns history; Export gone). Gate green, 883 tests.
Next session: user is live-testing the popover — if collapse-expand still comes back blank, pin the popover to a fixed size instead of content-sizing (see close reason on ccp-ua73). If placement reads wrong, the arrow flip is one word in NoteSurface syncStatus.
