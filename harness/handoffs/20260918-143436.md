# Codex support handoff

ccp-y7ih adds shared skill links, generated Codex roles/hooks and compatibility checks.
Claude skill bodies, reviewer prompts, settings and OpenCode definitions are unchanged; Codex mappings live in harness/codex.md.
Next Codex setup step: review/trust the new hook definitions in /hooks; until then run the brief manually.

Ten Codex regression checks pass. The quick gate passed before concurrent app edits; the final rerun fails in untracked ClaudeAppGrant.swift because NSWorkspace is not in scope. No app-source changes are part of this commit. The full suite ran 727 tests twice and still hits the existing ccp-sih9 shared-temporary-directory failure, including with isolated TMPDIR. Taste and correctness reviews were clear. Only this bead is included in the commit; other ledger and app edits remain separate.
