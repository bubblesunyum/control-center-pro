# handoff 2026-08-31

Closed ccp-k0d (a38b479) — clipboard right-click + status Edit/Quit, hold-to-edit removed.
Fixes kept after review: ClipboardRow is now full-row Button with .contextMenu + accessibilityActions, EditableCard drag is highPriorityGesture, StatusItem left/right split via rightMouseDown/0-point popUp, ControlPanelController.present(editing:) dedupes show and showAndStartEditing. Review still flags hidden clipboard affordance vs visible Menu and no in-panel Edit entry — intentional per user spec, mitigated with a11y actions.

Next is ccp-8ld.3 Quick Toggles or ccp-8ld.4/6 Now Playing/Audio — per brief order.
