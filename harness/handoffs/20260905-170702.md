# handoff 20260905-170702

Push works live but head insertion has no working spelling (ccp-gfe5 blocks ccp-2zi.5); running app is on 08cfd99, tree clean, gate 382 green.
Was mid-thought on nothing code-side — next is hypothesis 1 on ccp-gfe5 (pageId alongside siblingId), then the live probe. Don't type top-inserts until it resolves.
If the next session touches sync failures, remember the pad logs nothing by design; ccp-2zi.7 is the durable answer, not more TEMP logs.

Commits: 6bf4f80 (items envelope, start), 08cfd99 (before-sibling, write-back removal, credential cache, mid-flight dirty fix), plus ledger bookkeeping and the laurel/memory commit on top.
Ledger: ccp-2zi.5 still in_progress (blocked by ccp-gfe5, P1 bug, freshly filed with hypotheses + the return/shift+return splitter rule); ccp-vbka closed (removal, not a flip); ccp-2zi.6 noted (reverse direction absent as expected, pull unbuilt).
Live-proven this session: first-sync order, appends, items echo → sidecar seeds, both todo states, normalisation; write-back rewrite killed by the user and fully removed.
Session traps, all on the beads: mapping went to the decoy ControlCenterPro domain while the app reads com.controlcenterpro.ControlCenterPro (now a remember key); Keychain URL was missing and re-added; per-push Keychain reads prompted on focus loss, now cached per process with sync invalidation.
Playground doc state: merged top block from the start/before probes; pad holds the truth; the fix's first push should heal it, verify order before trusting it.
Suggested skills: none beyond workflow.
