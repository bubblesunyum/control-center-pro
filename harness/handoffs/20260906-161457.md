# handoff 2026-09-06

ccp-e8df is done in the tree but deliberately uncommitted: Package.swift carries a temp path pin to the local engine fork (bba7288, committed there) because push/tag is user-owned. Next: user pushes + tags 0.12.0-ccp.2, then flip the pin back to the URL and commit.
The trailing-space report was Craft confusion (user confirmed) — new memory ccp-trailing-space-blame-craft has the proof trail. Live dev app left running; user was mid-testing in it.
