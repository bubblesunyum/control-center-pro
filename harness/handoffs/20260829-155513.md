# handoff 2026-08-29

Closed ccp-ait.3 (GPLv3 licensing/docs). LICENSE, PATCHES.md, SPDX headers were
already in place; the work was rewriting README as the CCP fork README — fork
attribution, independent-UI statement, trademark disclaimer, honest in-dev
framing. Committed as e9cddfb. Ran verify + a local review pass; review was
right that "engine code is left unmodified" was false (PATCHES.md tracks real
patches) — reworded.

Next: ccp-lr7.1, the CCPWidget protocol — per the brief it's the keystone (the
layout model, registry, and "one conformer adds a widget" all hang off its
shape). ccp-v64 (fork bundle identity) is the only leftover from this bead.

## What I was mid-thought on

The no-Vorssaint-branding acceptance criterion is satisfied for CCP's own
sources (Sources/CCPKit, CCPUI, ControlCenterPro — clean), but the fork's
*build path* still emits a Vorssaint bundle: build.sh sets EXECUTABLE=Vorssaint
and APP_BUNDLE_ID=com.vorssaint.utils. Filed ccp-v64. It's upstream tooling so I
left it untouched, but it's unresolved if the fork ever ships a build.

Decided (cheap now, expensive later): the README frames the UI in future tense
("when complete it will present…") because nothing renders yet — the CCP
targets are stubs. That framing should flip to present tense the moment the
shell actually draws, or the README and the app will quietly disagree.

## Watch out for

- Neither origin/main nor the local Dolt pull has been pushed — beads live only
  in the local Dolt DB this session. `bd dolt push` when the user asks.
- ccp-ait.3 was reopened, re-reviewed, and re-closed after I skipped the review
  pass the first time; the loop is: verify → review → commit → close → capture,
  in that order, and the review uses local agents (general/explore), not the
  .claude/agents/ ones.
- Reminder from prior note still stands: origin = user's fork, upstream =
  vorssaint-utils; ask before merging upstream.
