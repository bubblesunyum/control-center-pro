# handoff 2026-09-19

Closed ccp-ecye (5067a6b, ca3d601): dismiss clicks are swallowed, verified live with user at 1am.
The swallowing monitor alone wasn't enough — fully-transparent window pixels are never delivered to the app at all, so the backdrop needed a 2% alpha veil plus a seat covering the Dock strip. Remembered as transparent-pixels-are-unclickable.
Still pass-through by design: menu-bar strip, side-Dock strip, other displays (global monitor dismisses only). ccp-sy9k holds the taste leftovers.

Uncommitted tree still has others' work (Claude OAuth files, Settings, ClipboardWidget, AppDelegate) — mine was only the 5 backdrop files across the two commits above. Running app (pid 77152) is the final clean build.
