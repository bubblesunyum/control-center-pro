# handoff 2026-08-31

Closed ccp-o8n (5581f3f + ed47a0c + 613d15e) — shelf floating panel copied from Vorssaint ShelfView (304×188, Radius.card 16, glass + move handle) with ShelfStore (tolerant decode, ghost-file guard, fileURLs .webloc, persist off-main) and ShelfWidget (GlassCard, well padding Space.one, duplicate Open removed, header owns toggle). Principal design reviewer now enforces wells/loud-state.

Was mid-thought on isVisible sync: stored @Observable isVisible fixed header toggle reactivity but diverges if AppKit orders panel out without hide() — needs NSWindow occlusion observer to stay truthful. Left as noted on ccp-ilk.

Next is ccp-3cq captures (headless couldn't screencapture — needs GUI session empty/6/scroll/large-type), then ccp-ilk AppKit split or ccp-8ld.3 Quick Toggles per brief order. App is running with --show-panel at http://localhost:7391 dashboard.

