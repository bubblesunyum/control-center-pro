# handoff 2026-08-29

This repo is NOT opencode-ready yet, deliberately — ccp-veh waits on har-gdz in
harness-starter so the contract arrives by install rather than being hand-written
here twice. Everything today was harness plumbing; the app didn't move.

## What landed here

- **ccp-038** — dropped the `bd prime --hook-json` SessionStart hook. It ran
  beside `brief.sh` rather than instead of it, ~1900 tokens against the brief's
  ~240, every session. `scripts/context.py` now fails the gate on any
  SessionStart hook the harness didn't install, across all three files Claude
  Code merges (`~/.claude/settings.json` included — `bd setup claude --global`
  writes there, and a check reading only the project file would report clean).
- **ccp-0wa** — removed the 53-line duplicate bd managed block from CLAUDE.md.
- **ccp-623** — picked up the rest of the gate; collapsed AGENTS.md's two bd
  blocks to one.

Always-loaded cost went 4022 → 4131, which reads like a regression and isn't:
AGENTS.md's ~790 tokens were always being paid by any agent reading it and were
never counted. CLAUDE.md itself went 3415 → 2728.

## What's waiting

- **ccp-veh** (P1) — the CLAUDE.md/AGENTS.md split. Do this *after* har-gdz.
  The agent-agnostic half is `template/AGENTS.md` in the starter; installing it
  is the point. What stays in CLAUDE.md: architecture, philosophy, naming,
  comments, animation, accessibility, testing, the upstream rules. What leaves:
  Review, Commits, Work tracking, Build & Test — the contract already says them.
- **ccp-ami** (P2) — the MEMORY.md dashboard note should be `bd remember`, so
  opencode can see it. AGENTS.md's new memory rule says exactly this; this repo
  is the counterexample sitting next to the rule.

## Worth knowing

Another session was committing in this checkout while I worked (3d28645). The
brief I woke up to also showed ccp-kcc and ccp-459 as in-progress when both were
already closed — worth distrusting the brief's in-progress list if it disagrees
with `bd show`.

Next I'd take har-gdz in harness-starter, then come back and do ccp-veh in one
pass. Nothing here is half-applied; the gate is green.
