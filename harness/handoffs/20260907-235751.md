# handoff 2026-09-07

Closed ccp-6cvu + ccp-2yy with one capability (ab77202): PanelVisibility +
`onPanelHidden`, because `orderOut` keeps the graph standing and three files
were already apologizing for the same missing hook in comments.

Was mid-thought on how far to push the adoption. ccp-gc3h holds the two
remaining leaks on that path; the SystemStats one is the interesting half —
its `onDisappear` resets `expanded`/`breakdownRows`, has never once run, so
what ships today is "your breakdown is still open when you reopen the panel."
Making the author's intent real would change what a reopen shows, which is
laurel's call (no-design-changes-without-asking), so I left it. Worth asking
rather than assuming the reset was right: an expanded breakdown surviving a
hide may be the better behaviour and the dead code the mistake.

The correctness reviewer earned its keep here — my first cut routed the hide
through StickyCard's old `onDisappear` body, which zeroes the drag transients,
and Esc mid-drag reaches `hide()` with the finger still down. Would have eaten
a sticky move and read exactly like the drag-feedback bug. Two new traps:
`bd recall hiding-the-panel-does-not-cancel-a-drag` and
`observation-skips-a-same-value-set`.

Next I'd pick up ccp-gc3h (after asking about the stats reset), or ccp-sqte —
Files' downloads go stale for the same family of reason: the store reloads in
`.task`, which the surviving view graph runs exactly once.
