# handoff 20260910-214710

Shipped ccp-xd9t (0307ecc): stickies trailing-edge anchored, gate 897 green, taste+correctness reviewed.
Tree was clean except ledger churn; this note rides with it — commit both if touching.
Next session: the one eyeball nobody has done is a real dock/undock with notes on the desk. Migration assumes the current seat width (see bd recall sticky-migration-assumes-current-width), so if the user's first post-update open is on a different display, expect a one-time shift by the width delta, then stable forever. Don't "fix" that unless it actually bites — it needs a recorded seat width we deliberately didn't add.

Rejected and still tempting: per-widget anchoring (the original ask) and shifting x by delta-W on seat change ("imitating right-anchoring"). Both lose to storing trailing offsets: zero monitor-change code paths, no width cache, no observer. The 10-finding review that looked scary collapses to one careful 15-line migration block plus "don't touch the lane cache."
