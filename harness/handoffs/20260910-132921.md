# handoff 2026-09-10

Closed ccp-71st (2a30c2c, 5fce9b7) — AI Usage card with OpenCode + Claude sections is live and the user is happy with it.
If the Claude section ever goes Import-only again an hour after import, that's the design, not a bug: no-refresh is load-bearing (single-use rotation would log their CLI out).
Next I'd pick up: ccp-19r8 (shared poller for the two usage adapters) or screenshot-verifying the 24pt section rhythm — the user eyeballed it live, no capture was taken.
