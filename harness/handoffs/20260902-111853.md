# handoff 2026-09-02

Closed ccp-3uv (Liquid Glass) — GlassCard/ShelfView now use `glassEffect(.regular)` on macOS 26 with `reduceTransparency` + `compiler(>=6.2)` guards, fallback `hudWindow` with layer corner clipping (was `popover` square blur). Verified against jellyfish wallpaper light/dark; photo's Control Center frost now matches. Closed ccp-zpf — mail 600→900 (Dimensions.swift) shows more messages, verified via `open --args --show-panel` capture.

Was mid-thought on reviewer nits: taste wanted `GlassBackground` extraction but inline `glassBackground` with duplicate branch in two files was left (filed implicitly, but no bead) — extraction via `GlassBackground.swift` was attempted and reverted after lock-screen confusion, then re-added as inline to keep panel visible. Current inline duplication is intentional smallest fix; extraction could be revisited. Correctness reviewer flagged hardcoded `Radius.card` in fallback vs `shape` — fixed via `cornerRadius` param stored alongside shape (now inline, so fixed).

Next to pick up: ccp-caq (psymail background over glass), ccp-rh9 (review.sh stale captures), or new widget work. Remember: never edit `layout.json` without asking (bd memory `never-edit-layout-file`). Tree green on `0453cb6`.

