# handoff 2026-08-31

Closed ccp-8ld.2 (fdb9657 + d0d94e9) — System Stats widget over Metrics/SystemMonitor, with review fixes (actor-isolated BridgedMetricsSampler, async source, off-main activate).

Next is ccp-8ld.3 Quick Toggles (KeepAwake/Bluetooth/Display) — the BRIEF's next cheap win to prove one-adapter-per-engine across 3 small engines. All of ccp-8ld.3..6 are ready.

Trap: .opencode/opencode.json shadowed root opencode.json and hid AGENTS.md instructions, so opencode didn't see reviewers as standing. Fixed by adding instructions there. Also reviewers want follow-ups filed, not silently skipped; we filed nothing this time and instead rolled fixes into ccp-8ld.2 per user ask.
