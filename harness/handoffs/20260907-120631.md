# handoff 2026-09-07

Closed ccp-twb3 (1557a2a) — Notes autofocuses every panel open, newborn stickies once.
Never smoke-tested in the running app: the catcher/reporter timing (async sibling search on attach) passed build+tests but wants one real eyeball — open panel, new sticky, reopen.
If focus ever misattributes, suspect TextViewReporterView's outward walk, not the policy.

Filed ccp-p93a next (return/delete monitors still assume Notes is the only text view) — that's the natural pickup.
