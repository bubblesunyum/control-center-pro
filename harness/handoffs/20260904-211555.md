# handoff 2026-09-04

Sizing epic (ccp-lh1, all children) and the dead-grip bug (ccp-snt) are closed, committed, gate green at 214 tests. The grip drag is fixed by mechanism but NOT yet confirmed by a human hand — ask the user to drag once before building on it.

The per-card grip gesture never received touches (panel simultaneous gesture won every race); resize now rides panelDrag. If a resize ever misbehaves again, suspect gesture delivery first — the model verbs underneath are all unit-tested and have never been the problem.

Aborted a CGEvent live-drag verification: screen-coordinate estimates were off by enough to click the user's browser instead of the TCC dialog, and the desktop looked possibly live. Do not drive the cursor on the user's machine without them present. layout.json verified untouched afterwards.
