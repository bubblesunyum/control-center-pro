# handoff 20260904-233004

Shipped live-follow resize + magnetic snap + roomier note text (bec5803, ccp-q4i, ccp-eeu); gate 239 green. User layout restored to (2,2) and store-verified after probes; app quit clean.
Still open in spirit: no clean synthetic proof of 1:1 tracking — ask the user to drag a grip themselves and report (taut follow? gentle snap near steps? clean snap on release?).
Rejected and why it still tempts: panel-space gesture translation — the growing window feeds back into it (bounded ~2x overshoot measured live), so resize is measured in NSEvent screen points instead; don't "simplify" back.

- Resize model: `fraction` (continuous, drawn via `drawSize` + `magnetized`) vs `span` (whole, packing + commit). Span math byte-identical to before; all old tests pass untouched.
- Window: direct `place()` in flight, animated settle on release/cancel (`trackResizeChanges`).
- Note editor: body 13→14pt, insets 8/4→12/8 (`MarkdownNoteEditor` config).
- Synthetic-verification hazards, now in `bd recall`: cgevent-ups-arrive-late-or-never, press-coords-go-stale (validate tick n≈260, abort otherwise), release-outside-panel dismisses it. Probes must be span-safe (no step-boundary crossing) so even a delivered release commits a no-op; verify commits via layout.json, never pixels.
- Next ready per brief: ccp-ydo epic tail (ccp-ydo.6), ccp-nqi follow-up polish ccp-8sa.
