# handoff 2026-09-15

Tiptap editor shipped for Notes + stickies (epic ccp-9vpv, 7/10 done, commits 10aef60 on). User dogfooded it: "way better". Next: ccp-9vpv.1 (run scripts/editor-gate/run.sh only while the user is away) then ccp-9vpv.5 (in-panel captures + reviewer-design).
Editor code lives in ../bb-editor (MIT, own git repo, not pushed): change it there, `npm test`, commit, then `scripts/editor-bundle.sh` here and commit the bundle (Resources/NoteEditor/VERSION stamps the commit).
Rule from the user: keep Tiptap's own behaviour; add custom behaviour only where a default loses text. ccp-9vpv.4's DESIGN lists what was deliberately NOT ported.

## Map
- bb-editor `src/core/markdown.js`: every data-safety rule (LineBreak, EmptyParagraph, EmptyListItem, RawHtml, MarkdownSafety patches on the markdown manager). `src/craft/index.js`: `***`, `<highlight>`, `fromBlockBoundaries` (pad `"  \n"` → markdown on load). `src/webkit/index.js`: `window.bbEditor` + `bbEditor` message handler. `scripts/roundtrip.mjs <Notes dir>`: read-only corpus check, masked output.
- CCP `Sources/CCPUI/NoteEditor/`: NoteEditorController (page bridge, per-document state, compare-and-swap replace, crash reload, file drops), NoteEditorStyle (CSS variables from design tokens), NoteEditor (view). `Sources/CCPKit/Craft/UntouchedBlocks.swift`: puts stored bytes back for unedited blocks. `Sources/CCPUI/Stickies/StickyEditorController.swift`: one roaming sticky editor + offscreen snapshots.
- No pad dialect migration happened (ccp-9vpv.7 close reason): pads keep `"  \n"` from pulls, editor saves use blank lines, the splitter reads both.

## Loose ends I couldn't pin down
- Stickies' roaming editor is unit-tested for snapshots only; focus hand-off between stickies (resign → snapshot → park → mount → posAtCoords) has never been clicked through by me. Watch for a sticky that goes blank or loses the caret on the second click.
- One real note (fences inside a list) isn't a fixed point on reload; harmless while UntouchedBlocks holds it, but it's the shape to test if saves ever churn.
- Rail tap focus: assumed SwiftUI buttons don't take first responder from the web view; if the rail flickers away on tap, that's why.
- `scripts/app.sh --launch` bare still failed (-600) this session despite ccp-qahu; `--show-panel` works.
- At handoff the gate could not build: "You have not agreed to the Xcode license agreements" (Xcode seems to have updated mid-session). The last passing gate was on 10aef60 (882 tests). Since then only the CSS bundle and scripts/editor-gate changed, and the app built fine after the bundle change. Run `sudo xcodebuild -license`, then `scripts/verify.sh`.
