# handoff 20260903-233706

Investigated why `bd` was eating the machine: it wasn't CCP, it was the harness
dashboard. Fixed across all four installs (ccp-4pu ccp-juo ccp-o25, har-l63
har-3zd har-3hx har-b68, bbk-71n, psy-50b) — 207 bd spawns/60s down to 21, ~67%
of a core down to ~5%.

Was mid-thought on the one thing I measured but didn't chase: a handful of
short-lived processes each fork ~4 `bd` children and then exit, so they never
resolve to a parent in a PPID sample. They're the bulk of what's *left* — the
four dashboards themselves now account for about a third of the 21. Could be bd
spawning dolt helpers, could be `export.auto = true` writing issues.jsonl on
read paths. If someone wants the last 5%, that's where it is.

The thing that still nags: I chose not to port the refresher-thread generation
into psymail-mini (psy-an5). Measured, that's right — it builds on the request
so its idle cost is genuinely zero. But it means psymail-mini is now the only
install where a poll can wait on `bd`, which is the exact failure the refresher
was written to fix, and the divergence gets more expensive every time the
template moves. Tempting to just do it; the 505 lines of drift are why I didn't.

Also noticed and left alone: psymail-mini's dashboard still calls
`webbrowser.open` (scripts/dashboard.py:958,970), so it opens the *system*
browser, against the standing preference for Claude Code's pane. Noted inside
psy-an5, but it's a two-line fix that doesn't need the generation upgrade.

Next I'd pick up ccp-skd — inbox list clipped under the floating psymail tab
bar, no bottom inset. It's been ready a while and it's visible every time the
mail widget opens.
