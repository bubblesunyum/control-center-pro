# handoff 2026-09-08

Next is ccp-2zi.4 (P0): extract CraftNoteDestination behind the step-7 invariant — no Craft noun escapes into adapter/UI. The plan (docs/notes-storage-plan.md §3 step 7, §4.1) still holds after tonight; bookkeeping keys were untouched by the files move.

Closed tonight: ccp-2zi.4.2 + ccp-q3nd (`384cb65`, gate 730 green). Notes are files+index now; migration ran on the real vault (5 pads) and verified. Filed: ccp-6cji (crash-marker journal — the one thing review asked for twice), ccp-apma (backup misses files+index), ccp-9rgq (stray-file adopt).

Two things the ledger doesn't say: (1) no app window would come on screen this whole session — panel and Settings captures both returned wallpaper, so the new Settings → Notes row has never been eyeballed; worth a look before much more UI lands on it. (2) saveIndex() reads lastSavedDocument, not the live document, and that ordering is load-bearing — the memory persist-then-apply-indexes-stale-document has why. The step-7 extraction moves the sync accessors; keep the index writes on the committed side of that move.
