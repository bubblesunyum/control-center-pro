# handoff 2026-09-10

Pad editing landed (c703747, ccp-lype) and the fork tag is pushed, so the tree
resolves from a clean checkout again: `Package.swift` pins `0.12.0-ccp.4`,
`Package.resolved` matches it, the `swift package edit` override is gone, and
the gate is green at 834 tests. Nothing is left hanging.

The thing I kept getting wrong all session, and would warn the next person
about: the two-space boundary marker is real text, and every rule written about
it has to say what happens when the marker ISN'T there. `HardBreak.normalized`
sheds a space-only line on read and Craft strips trailing spaces on write, so a
freshly-opened empty block and a saved one are different strings that must
behave identically. Three of the four bugs the user found were that same gap in
a new place. `bd recall an-empty-block-cannot-hold-a-soft-break` is the related
constraint I hit and could not design around.

Round 3 of the plan — the selection format bubble, `ccp-z0a` — is untouched and
is what I would pick up next. It needs no fork work: `MarkdownEditorBus` is
already public and already carries every format command plus the
selection-state notifications; CCP wires none of it.

Two loose ends I did not chase. The caret snap runs off
`didChangeSelectionNotification`, so clicking past the end of a paragraph shows
one frame of the caret sitting in the invisible spaces before it corrects —
fixing that means intercepting placement rather than correcting it, which is a
different mechanism. And `ccp-3pou`: a notes-storage test read the user's REAL
pad text once and never again; probably the running app writing the same folder
underneath it, but "probably" is doing work there.

I never saw the editor rendered. Screen capture returns wallpaper on this
machine (`adhoc-builds-lose-tcc-grants`), so every visual claim this session was
arithmetic the user checked by eye. Reviewer findings against the prior
session's uncommitted EditPill work — 23 TEMP-DEBUG log calls, `pillDebug`
defined three times, and a `presets: [15, 25, 54]` that reads as a typo for 50
and is off the slider's 5-minute grid — are relayed but untouched; those files
are not mine to commit.
