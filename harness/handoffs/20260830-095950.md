# handoff 2026-08-30

Shipped ccp-lr7.6 and ccp-pr6 in 7959c69 — the panel now draws what is on disk.
Owed: the screen-clamp capture. Design review passed everything it could see but
flagged that no shot exercises sizeFitting; I built a 10-lane layout to force it
and both captures came back as the login wallpaper — the Mac was locked, which
looks exactly like the app failing to draw (remembered as a trap).

Two calls I made that laurel delegated and should still get a look:
normalized() dedups across the *whole* layout, not per-lane — a widget is placed
once, full stop. And launch writes layout.json back every time, which is the only
reason "persists across relaunch" is testable today; it also means normalizing is
a silent one-way edit of someone's file the first time they upgrade into a build
that dedups. That felt slightly wrong and I couldn't make it feel wrong enough to
stop — worth a second opinion before edit mode makes writes frequent.

Next I'd take ccp-lr7.7 (edit mode). It is the first thing that *writes* a
layout, so ccp-mtk (save debounce, off-main) and ccp-p6g (a panel wider than the
screen is cut, not reflowed) both come due as it starts rather than after. Decide
those two first; retrofitting a drag that can build an unrenderable layout is
worse than refusing the drop.
