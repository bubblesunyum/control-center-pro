# handoff 2026-09-15

Launch crash (ccp-2esx) fixed in c337f6b, but the user's running instance is still the pre-fix build — needs a relaunch (offered, not taken yet; relaunch drops a half-written mail reply, so let them pick the moment).

Next: ccp-lqpk (sticky commit paths preserve absurd coordinates, file never heals). Gate still red on 2 pre-existing env failures (proven via stash, unrelated). Repro technique that worked: silent XCTest hosting the real panel (see StickyDeskSeatTests); lldb objc_exception_throw + _subtreeDescription dumps.
