# handoff 2026-09-05

Closed ccp-yjqo (OpenCode usage widget) and ccp-0drm (percent precision via ledger spend). Tree green at e386a98, 313 tests.

What the bead reasons don't hold: monthly-window semantics are a guess that current data cannot confirm — trailing-30d, calendar-month, and since-subscription all agree while every dollar of spend is days old. If precision complaints return after Oct 5 (the monthly reset), re-derive the anchor from the new resetsAt before touching the query. Same for rolling: session-bucketed trailing-5h carries ~$0.02 of boundary/live-session noise that no local query can remove; the floor(local)==server check in ccp-0drm's notes is how to re-validate.

Next: ccp-5p1q (backlog) — upstream float-precision ask would retire OpenCodeSpendStore.
