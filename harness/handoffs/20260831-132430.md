# handoff 2026-08-31

Closed ccp-8ld.5 (a46c7f0) — clipboard history widget over bridged clipboard engine, plus fixes ccp-6qp/ccp-vn4 for enable button (isHistoryEnabled not observable, ensure vs forced enable, Defaults.register missing so isAvailable false). Always-relaunch remembered per user.

Next is ccp-8ld.3 Quick Toggles (cheap win) or ccp-8ld.4/8ld.6 Now Playing/Audio — per user, clipboard was jumped ahead of build order, so sequence is off. Trap: AppDelegate must call BridgedDefaults.register() or no feature starts.
