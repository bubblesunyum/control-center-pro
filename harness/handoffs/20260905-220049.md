# handoff 2026-09-05

Closed ccp-0gek (5447a55) — provisioning is lazy auto-create, no setup UI, per the user's reframing mid-session.
Next: ccp-2zi.5's live A/B (push+pull proven on throwaway 07EDE013; provision path itself untested live — first real edit will mint a real doc), or ccp-2zi.7 sync-status header.
Watch: `bd dolt push` still RPC-400s (ccp-oicx); ledger remote drifts, jsonl is committed. Test pad maps to throwaway in the stale `ControlCenterPro` defaults domain; real mapping lives in `com.controlcenterpro.ControlCenterPro`.
