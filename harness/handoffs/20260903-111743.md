# handoff 2026-09-03

Closed ccp-rz5 (b6e132b) — hold-edit-drag now panel-level with HeaderFramePreference so the same 0.5s hold lifts and drags without re-click; gallery overlay blocks panelDrag and uses GlassCard(isRaised:true) + ScrollView; menu rebuilds synchronously before popUp and checkmark preserves gallery via finishEditingPreservingGallery; wiggle 0 when inactive + edge antialiasing.

Reviewers on uncommitted tree: taste flagged PanelEditor public widening / isShowingGallery in model, design-system literals, and WidgetHeader primitive reaching into shell; correctness flagged holdTask leak on system cancel (now fixed via isGestureActive + onDisappear) and gallery not blocking drag (now guarded) and menu stale (now rebuild before popUp); design flagged missing loud-state captures and gallery clipping (now ScrollView maxHeight 360). Deferred taste/design polish filed as ccp-2rh, ccp-srw, ccp-urk — extraction of hold modifier, tokenizing 0.28 dim/0.5s/4pt, and PanelEnvironment file split.

Was mid-thought on whether the next hold after a system-cancelled drag still needs explicit holdTask nil on hide (added) and whether the menu-bar checkmark should use checkmark.circle vs checkmark.circle.fill (switched to non-fill per design). Next: run scripts/capture.sh --edit-mode and --show-panel to verify loud state, gallery, and checkmark at 2×, and close the three deferred beads.
