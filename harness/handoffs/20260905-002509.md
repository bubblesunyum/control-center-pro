# handoff 20260905-002509

Shipped the fork instead of the upstream ask: 0.12.0-ccp.1 exposes the block AST, xgl is unblocked, jqj closed superseded. Auth (2zi.3) also shipped tonight with 21 tests.
Next pickup is ccp-xgl (splitter + sidecar, entry point proven by EngineBlockASTTests) — but it was designed around swift-markdown ranges, so first line of work is re-reading its design against DocumentAST.parse(_:extensions:) before writing code. o3k after.
Still open in spirit: fork main keeps moving (directives etc.) while our tag sits on 0.12.0 — fine until we want something up there, then it's a deliberate merge, not drift.
